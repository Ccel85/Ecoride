{% extends 'base.html.twig' %}

{% block title %}Modification de profil{% endblock %}

{% block body %}
<div class="container p-4">
    <h1 class="text-center p-4">Modification de profil</h1>
    <div class="row">
        <div class="form-group {# w-75 w-md-50 #} col-sm-10 m-auto">
            {{ form_errors(form) }}
            
            {{ form_start(form, {'attr': {'enctype': 'multipart/form-data','method': 'POST' }} ) }}


            {{ form_row(form.nom, {
                'label': 'Nom',
                'attr': {'class': 'form-control'}
            }) }}

            {{ form_row(form.prenom, {
                'label': 'Prénom',
                'attr': {'class': 'form-control'}
            }) }}

            {{ form_row(form.pseudo, {
                'label': 'pseudo',
                'attr': {'class': 'form-control'}
            }) }}

            {{ form_row(form.email, {
                'label': 'Email',
                'attr': {'class': 'form-control'}
            }) }}

            {{ form_row(form.observation, {
                'label': 'Observations',
                'attr': {'class': 'form-control'}
            }) }}
            {# <div class="d-flex align-items-center justify-content-center">
                {% if utilisateurs.imageName %}
                    <img src = "{{ vich_uploader_asset(utilisateurs, 'imageFile') }}"
                    alt="img de profil" class="rounded-circle m-2" width="100" height="100">
                {% endif %}
                <div class="mb-2 w-100 text-center">
                    {{ form_row(form.imageFile, {
                        'attr': {
                            'class': 'form-control-file d-inline-block '
                        }
                    }) }}
                </div>
            </div> #}
        <div class="d-flex justify-content-start align-items-center">
            {% if utilisateurs.imageName %}
                <div class="">
                    <img src="{{ vich_uploader_asset(utilisateurs, 'imageFile') }}"
                    alt="img de profil" class="rounded-circle" width="100" height="100">
                </div>
            {% else %} 
            <div class="">
                    <img src="{{ asset('logo/Logo.png') }}"
                    alt="img de profil" class="rounded-circle" width="100" height="100">
                </div>
            
            {% endif %}
            <div class="col-sm-8">
                {{ form_row(form.imageFile) }}
            </div>
        </div>
            <div class="d-flex justify-content-evenly">
                <button type="submit" class="btn btn-success">Enregistrer</button>
                <a class="btn btn-success" href="{{ path('app_profil') }}" >
                Retour</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
